# requirements.txt — libraries need in for the project
# I tested with Python 3.10 à 3.13
# only used these ones to train the model
numpy>=1.26
pandas>=2.1
scikit-learn>=1.3
# these ones are mandatory
joblib>=1.3
matplotlib>=3.8
scapy>=2.5

# Confort (optionnel)
tabulate>=0.9
tqdm>=4.66